# Snapshot placeholder for claude-config-change-gate.
# This file is populated by the base JSON comparator (json-content-compare-ignore-keys)
# on first run. It stores the SHA-256 hash of the filtered known_marketplaces.json.
# On fresh clones, setup-hooks.bash seeds it from scripts/reference/known_marketplaces.json.
