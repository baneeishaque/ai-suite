#!/bin/bash
# Thin wrapper: exec lib.bash with the hook name.
#
# Tier: 2 (Bash) — thin shell wrapper.
# See scripting-language-selection-rules.md section 3.4.

exec bash "$(git rev-parse --show-toplevel)/scripts/githooks/lib.bash" "$0"
