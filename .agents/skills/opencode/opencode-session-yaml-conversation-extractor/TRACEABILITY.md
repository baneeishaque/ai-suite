# Traceability

- Origin: session `ses_012fd48f0ffedPT1brWW8fcezW` (2026-08-11) — created as a
  parallel base to `opencode-session-yaml-transcript-extractor` after the
  conversation-only YAML transcript extraction workflow demonstrated that the
  existing JSONL-all-fields transcript extractor serves a different contract
  (machine analysis) and cannot be enriched without breaking its composer
  (`opencode-session-problem-solution-workflow-analysis`).
- Source of truth for the YAML layout: the opencode logger-plugin artifacts
  under `.opencode/logs/`.
- Created 2026-08-11.
