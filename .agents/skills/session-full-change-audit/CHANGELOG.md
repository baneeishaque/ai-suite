# Changelog — session-full-change-audit

## v2 (2026-07-31)

- Added `--yaml <path>` mode — audits opencode logger-plugin YAML logs
  (monolithic `.yaml` file OR per-turn `ses_<id>/` directory) by running
  every extractor in `--yaml` mode; `.md` mode unchanged.
